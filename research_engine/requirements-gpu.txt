# GPU 推理与模型依赖（在 basketball_classroom conda 环境中安装）
# PyTorch 请通过 setup_gpu_env.sh 安装 cu124 版本

# 核心（与 requirements.txt 重叠）
pyyaml>=6.0
numpy>=1.24,<2.0
opencv-python-headless>=4.8
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
jinja2>=3.1
tqdm>=4.66
requests>=2.31
ultralytics==8.3.181
huggingface_hub==0.27.1

# ONNX GPU 推理（不要同时安装 onnxruntime CPU 版）
onnxruntime-gpu==1.19.2

# 人体检测 + 全身133点姿态（RTMPose/RTMW）
rtmlib==0.0.13

# 人脸识别 ArcFace
insightface==0.7.3
onnx==1.17.0

# 身体 ReID（OSNet 推理；完整 CLIP-ReID 微调见 docs/环境配置GPU.md）
torchreid==0.2.5
tensorboard==2.18.0
gdown==5.2.0

# MotionBERT / RTMW3D are intentionally excluded from the first product runtime.

# Pose2Sim（多视三角化，按需）
# pose2sim>=0.10
