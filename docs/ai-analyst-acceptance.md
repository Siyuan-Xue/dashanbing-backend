# AI 分析师真实验收 · 2026-09-05

本次在隔离的本地预览中使用真实 GLM-5.3 Chat Completion，参数保持思考开启、max、temperature 1、max_tokens 65536
Key 仅保存在权限 0600 且被 Git 忽略的服务端配置，自动化测试不读取本地部署配置

## 样例与报告

| 样例 | 原始动作 | 汇总投篮结果 | 真实报告 |
| --- | --- | --- | --- |
| quick-demo | 4 次跳投 | 2/4，无未关联结果 | 中文/英文，教练/锐评 |
| mixed-actions | 15 次三威胁、12 次跳投 | 4/12，3 个结果无法可靠关联片段 | 中文/英文，教练 |
| verified-outcome | 18 次罚球动作 | 17 次有结果，10 中、7 未中，另有 1 次动作结果未定 | 中文/英文，教练 |
| layup-demo | 6 次上篮 | 汇总 5/6，其中 3 个结果无法可靠关联片段 | 中文/英文，教练 |

共 10 份真实报告，通过结构、来源摘要、球员归属和视频引用校验
无法关联的片段保留结果未定，不把汇总数据强行分配给个人或片段
现有样例没有通过姿态质量门槛，报告不能据此判断具体技术缺陷；本地未重新运行 GPU 推理

首次真实生成暴露出补拍建议及为调侃虚构时长的问题，已修正提示词并重新生成相关报告
报告和追问属于生成式解释，仍须结合视频证据复核

## 真实交互

快速示例的教练追问在 10.4 秒内完成，收到 13 次持久化消息更新
验证了重复提交返回同一作业、完整回答刷新恢复、引用只指向本场四个事件
真实浏览器另外检查五视角自动播放、按当前机位时间偏移定位、球员与风格切换

## UI 与首页

结果顺序为视频、概览/时间线/JSON、AI 分析师，数据标签切换保持区域高度
分析师常驻显示，所有选择器收进与任务筛选面板一致的配置浮层
报告和追问上下排布，报告内部按关键片段和训练建议分栏，追问空状态不占用固定高度
配置面板内选择器统一为紧凑等宽，只在原数据区与分析师之间设置语义分割线
真实视频检查发现本地 FFmpeg 搜索路径缺失，已提供本机现有可执行文件，四路原视频与合成视频均已实测播放通过
移除页面底部模型名称、生成时间和重复技术说明，后端来源记录继续保留

首页使用同一快速示例的真实篮球画面和分析师截图，中文/英文、浅/深主题、桌面/手机共 16 份 WebP
截图取自新界面，视频与分析师分别裁切，不拼接改造原始页面
静态素材清单位于 `frontend/public/assets/previews/analyst/manifest.json`，包含原报告 ID 和事实摘要
首页不调用模型，截图可通过 `frontend/scripts/capture-analyst.mjs` 重新采集

## 回归结果

- 后端 509 项通过
- 前端单元测试 124 项通过
- 分析师关键 E2E 24 项通过，20 项为重复视口矩阵跳过
- 截图校验与原生布局测试 6 项通过
- TypeScript 检查、前端生产构建通过

本次只更新本地预览与代码，没有生产部署
真实报告保存在配置的 `runtime_root/analyst-presets`，本地预览使用 `runtime/analyst-preview/analyst-presets`
部署时需随同迁移这些已验证报告；重新生成报告后，应重新采集截图以保持报告 ID 和内容一致

## 独立对比与确认绑定的后续验收

本场报告改为只依据本场事实，档案绑定、备注变化和追加对比均不改写原报告
旧报告迁移保留正文、ID、时间和作业，读取时校验旧事实快照后复用
绑定弹窗统一确认所有球员和球队，取消不保存，档案类型匹配并禁止同一球员档案重复绑定
配置仅保留风格，历史对比通过独立入口按需追加，刷新恢复，失败单独重试
新建档案中的笔记更名为“补充备注”，可留空，用于追问及独立对比

使用 quick-demo 和 mixed-actions 的真实输出在隔离测试库完成一次 GLM-5.3 对比生成
测试分组仅用于验证流程，不认定样例属于同一真实球队，不作为宣传素材
原报告的数据库快照在生成前后相同，独立报告使用 8588 tokens，正确说明汇总指标不可直接比较
这一检查不修改生产用户、档案或训练记录

新增迁移 0010 仅增加报告类型与历史记录标识字段，OpenAPI 与前端类型已同步
本轮后端 563 项通过，前端 196 项通过，类型检查与生产构建通过
浏览器最终回归 34 项通过，24 项为重复设备矩阵跳过，覆盖默认不生成、统一绑定、选择对比、追加多份报告与刷新恢复
追问目前不自动继承刚追加的对比对象，原报告也不因补充备注变化而改写
改绑后单独刷新被撤销的聊天回答，保留原报告与尚未发送的问题，已加入回归验证
